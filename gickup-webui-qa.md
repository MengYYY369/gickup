# Gickup WebUI 问答汇总

## Q1

**问题：WebUI 的首要目标**：这个 WebUI 最核心要解决什么问题？

**用户最终答案：为现有配置提供可视化编辑器，替代手写配置文件**

## Q2

**问题：目标用户**：谁会使用这个 WebUI？

**用户最终答案：仅本机管理员或开发者**

## Q3

**问题：首版操作范围**：除了编辑配置，首版是否需要直接触发 gickup 的实际操作？

**用户最终答案：只编辑、校验、导入和导出配置**

## Q4

**问题：配置权威来源**：WebUI 与现有配置文件之间应是什么关系？

**用户最终答案：现有配置文件仍是唯一事实源，WebUI 直接读取和写入它**

## Q5

**问题：部署边界**：你希望 WebUI 以什么形式运行？

**用户最终答案：由 gickup 自身启动 HTTP 服务并托管前端**

## Q6

**问题：兼容性要求**：新增 WebUI 后，现有 CLI 和无界面部署应如何处理？

**用户最终答案：CLI 必须保持完全可用，WebUI 是纯增量能力**

## Q7

**问题：首版成功标准**：什么结果意味着第一版已经值得使用？

**用户最终答案：能无损加载、编辑、校验并保存项目支持的全部配置**

## Q8

**问题：“无损”的具体范围**：加载并保存已有 YAML 时，WebUI 必须保留哪些内容？

**用户最终答案：保留注释、键顺序和未知字段，允许格式化空白与引号**

## Q9

**问题：多配置文件范围**：Gickup CLI 支持同时传入多个配置文件。首版 WebUI 应管理哪些文件？

**用户最终答案：允许浏览和管理某个目录内的所有 YAML 文件**

## Q10

**问题：YAML 多文档呈现**：一个配置文件可以由 `---` 分隔为多个配置文档。WebUI 应如何呈现？

**用户最终答案：左侧文档列表，右侧编辑当前文档，并支持新增、复制、排序和删除**

## Q11

**问题：文档识别方式**：现有配置文档没有稳定 ID。列表中应使用什么名称？

**用户最终答案：根据顺序自动命名为“配置 1、配置 2”，并显示来源与目标摘要**

## Q12

**问题：创建和删除能力**：首版除了编辑现有内容，是否允许改变文档集合？

**用户最终答案：允许新增、复制、排序和删除 YAML 文档**

## Q13

**问题：保存时机**：表单修改应如何写回配置文件？

**用户最终答案：用户点击“保存”，保存前显示校验结果和变更摘要**

## Q14

**问题：并发修改冲突**：用户可能在 WebUI 打开期间使用编辑器修改同一 YAML。保存时如何处理？

**用户最终答案：读取时记录文件版本；若磁盘已变化则拒绝覆盖，并要求重新加载或查看差异**

## Q15

**问题：保存安全机制**：写入正式配置时需要什么保护？

**用户最终答案：同目录临时文件、完整校验、原子替换，并保留一个上一版本备份**

## Q16

**问题：Schema 的权威级别**：`gickup_spec.json` 当前主要用于编辑器提示，且与 Go 运行时规则存在差异。首版如何定位它？

**用户最终答案：Schema 负责结构校验，Go 后端追加跨字段和运行时语义校验**

## Q17

**问题：Schema 不一致处理策略**：在 WebUI 开发前，是否先处理 `gickup_spec.json`、Go 类型和示例配置之间的差异？

**用户最终答案：先建立一致性测试并修复会影响编辑或保存的差异，再开发表单**

## Q18

**问题：敏感值编辑行为**：配置中可能包含 token、密码、SSH 密钥路径，也可能通过环境变量或 token 文件间接提供。WebUI 应如何处理？

**用户最终答案：保留原始表达方式；敏感明文默认遮罩，除非用户主动操作，否则不回显或改写**

## Q19

**问题：未知字段策略**：现有 Go YAML 解码器可能忽略未知字段，而 Schema 会拒绝它们。WebUI 发现未知字段时应如何处理？

**用户最终答案：保留字段，在对应文档中给出警告，并提供只读的原始 YAML 定位信息**

## Q20

**问题：原始 YAML 入口**：RJSF 无法自然表达所有 YAML 特性。首版是否提供原始 YAML 编辑模式？

**用户最终答案：提供只读 YAML 预览和保存前差异，不提供自由编辑**

## Q21

**问题：WebUI 启用方式**：WebUI 应如何启动？

**用户最终答案：使用独立子命令，例如 `gickup webui`**

## Q22

**问题：WebUI 运行期间是否执行备份**：启动配置管理界面后，进程是否还应按当前配置运行 cron、Webhook 和备份任务？

**用户最终答案：不执行，WebUI 子命令仅提供配置管理服务**

## Q23

**问题：本机访问边界**：既然首版仅供本机管理员使用，服务应如何监听？

**用户最终答案：默认只监听 `127.0.0.1`，允许 CLI 参数修改端口，但不允许首版绑定非回环地址**

## Q24

**问题：本机认证**：只允许回环访问的首版是否仍需要登录或管理 token？

**用户最终答案：不需要，依赖操作系统账户和回环网络边界**

## Q25

**问题：前端交付形态**：前端构建产物如何随 Gickup 发布？

**用户最终答案：使用 `go:embed` 打入现有二进制，由同一 HTTP 服务托管 SPA 和 API**

## Q26

**问题：前端工程栈**：RJSF 之外，首版采用哪套基础工程？

**用户最终答案：React、TypeScript、Vite、`@rjsf/core`、`@rjsf/validator-ajv8`**

## Q27

**问题：RJSF 主题**：首版使用哪个主题？

**用户最终答案：RJSF 默认 Bootstrap 3 主题**

## Q28

**问题：界面语言**：首版 WebUI 使用什么语言？

**用户最终答案：界面英文，但保留国际化结构**

## Q29

**问题：移动端范围**：本机配置工具需要达到什么响应式水平？

**用户最终答案：只支持桌面浏览器**

## Q30

**问题：浏览器支持范围**：首版需要支持哪些浏览器？

**用户最终答案：仅当前 Chromium 浏览器**

## Q31

**问题：配置目录来源**：`gickup webui` 应如何确定要管理的目录？

**用户最终答案：默认当前工作目录，可通过 CLI 参数覆盖**

## Q32

**问题：目录扫描深度**：WebUI 是否递归发现子目录中的配置文件？

**用户最终答案：只管理指定目录的直接子文件**

## Q33

**问题：配置文件匹配范围**：哪些文件应出现在配置列表中？

**用户最终答案：`*.yml` 和 `*.yaml`**

## Q34

**问题：符号链接处理**：目录中可能存在指向目录外部文件的符号链接。WebUI 应如何处理？

**用户最终答案：忽略所有符号链接**

## Q35

**问题：新建文件能力**：除了管理已有 YAML，WebUI 是否允许在配置目录中创建新文件？

**用户最终答案：允许创建，文件名由用户输入并严格限制在配置目录内**

## Q36

**问题：文件级操作**：首版是否允许重命名、复制和删除整个配置文件？

**用户最终答案：允许新建、重命名、复制和删除**

## Q37

**问题：删除文件保护**：删除整个配置文件时采用什么恢复机制？

**用户最终答案：移动到配置目录下的 `.gickup-trash`，允许恢复**

## Q38

**问题：备份文件命名**：每次保存保留一个上一版本时，采用什么形式？

**用户最终答案：同目录 `<filename>.bak`，下一次保存覆盖旧备份**

## Q39

**问题：空目录初始体验**：指定目录中没有配置文件时，WebUI 应提供什么起点？

**用户最终答案：提供“从示例创建”和“创建空白配置”两个入口**

## Q40

**问题：示例配置来源**：选择“从示例创建”时，模板从哪里获得？

**用户最终答案：将当前 `conf.example.yml` 随二进制嵌入**

## Q41

**问题：配置文件列表信息**：文件列表首版展示哪些状态？

**用户最终答案：文件名、文档数量、最后修改时间、校验状态和未保存状态**

## Q42

**问题：外部文件变化处理**：当用户正在编辑，而磁盘文件被外部程序修改时，界面应如何响应？

**用户最终答案：检测到变化后立即提示，但保留页面中的未保存修改；禁止保存直至用户选择重新加载**

## Q43

**问题：冲突时的差异展示**：发现外部修改后，需要展示什么差异？

**用户最终答案：展示“页面基准版本”和“当前磁盘版本”的只读 YAML 差异**

## Q44

**问题：保存前变更摘要**：用户点击保存后，如何呈现变更？

**用户最终答案：显示 YAML 统一差异，用户再次确认后写入**

## Q45

**问题：校验失败时的保存规则**：JSON Schema 或 Go 后端语义校验失败时，是否允许写盘？

**用户最终答案：不允许覆盖正式文件，但允许下载或复制当前 YAML 草稿**

## Q46

**问题：现有无效配置处理**：打开一个 CLI 当前可能接受，但不满足 Schema 的旧配置时，应如何处理？

**用户最终答案：允许打开和查看，保留全部内容；修复错误前禁止保存**

## Q47

**问题：Go 后端校验深度**：保存前的服务端校验应做到哪一层？

**用户最终答案：YAML 解码、Schema 结构、cron 和本地跨字段规则，不访问外部服务**

## Q48

**问题：Schema 一致性测试范围**：开发前建立的一致性测试应覆盖什么？

**用户最终答案：Go YAML 字段与 Schema 属性、类型、必填项、枚举、默认值说明及示例配置**

## Q49

**问题：默认值写入策略**：RJSF 可能为缺失字段填充默认值。保存时应如何处理？

**用户最终答案：只保存用户明确设置的值，不因打开表单而批量写入默认值**

## Q50

**问题：字段清空语义**：用户清空一个可选字段时，YAML 中应如何表示？

**用户最终答案：删除该键**

## Q51

**问题：数组项目操作**：来源、目标和过滤列表在表单中应支持哪些排序能力？

**用户最终答案：支持新增、复制、删除和拖动排序**

## Q52

**问题：复杂字段编辑器**：首版需要哪些专用组件？

**用户最终答案：cron 编辑与说明、secret 遮罩、文件路径文本输入、枚举选择，其余使用 RJSF 默认组件**

## Q53

**问题：原始 YAML 的敏感值展示**：只读 YAML 预览和差异中可能出现明文 token，应如何显示？

**用户最终答案：默认遮罩敏感值，可通过一次性操作临时显示**

## Q54

**问题：Edge 支持定义**：你选择仅支持 Edge。是否将兼容目标固定为哪一种？

**用户最终答案：当前稳定版 Microsoft Edge**

## Q55

**问题：CLI 参数形态**：已选择“默认当前工作目录、允许覆盖”。具体命令接口采用哪种形式？

**用户最终答案：`gickup webui --config-dir <path>`，省略时使用当前工作目录**

## Q56

**问题：监听端口**：WebUI 默认使用哪个端口，并如何覆盖？

**用户最终答案：默认 `6175`，允许 `--port` 覆盖**

## Q57

**问题：启动浏览器**：执行 `gickup webui` 后是否自动打开 Edge？

**用户最终答案：不自动打开，只输出 URL**

## Q58

**问题：HTTP API 风格**：前后端之间首版使用什么 API 形态？

**用户最终答案：同源 JSON REST API，使用 `/api/v1` 前缀**

## Q59

**问题：浏览器侧数据形态**：后端向 RJSF 返回什么数据？

**用户最终答案：返回结构化 `formData`、Schema、UI Schema，以及独立的 YAML 元数据**

## Q60

**问题：编辑会话状态**：后端是否需要保存长期编辑状态？

**用户最终答案：不保存长期会话；每个响应携带文件版本令牌，保存时提交表单数据和版本令牌**

## Q61

**问题：版本令牌实现**：乐观并发控制依据什么判断文件是否变化？

**用户最终答案：对读取到的原始文件字节计算 SHA-256**

## Q62

**问题：路径标识方式**：API 中如何标识配置文件，避免任意路径访问？

**用户最终答案：使用相对配置根目录的规范化文件名，后端每次解析并验证边界**

## Q63

**问题：隐藏文件处理**：`.example.yml` 等以点开头的 YAML 是否出现在列表中？

**用户最终答案：忽略所有隐藏文件和隐藏目录**

## Q64

**问题：大小限制**：配置文件和请求体是否设置上限？

**用户最终答案：单文件和 API 请求体均限制为 5 MiB**

## Q65

**问题：文件编码与换行**：保存时如何处理已有文件编码和换行？

**用户最终答案：只接受 UTF-8；保留原有 LF 或 CRLF，新增文件使用 LF**

## Q66

**问题：文件权限保留**：原子替换后如何处理原文件权限？

**用户最终答案：保留原文件权限；新文件使用仅当前用户可写的默认权限**

## Q67

**问题：备份恢复入口**：`<filename>.bak` 应如何恢复？

**用户最终答案：文件页面提供“查看备份差异”和“恢复上一版本”，恢复前再备份当前版本**

## Q68

**问题：垃圾箱生命周期**：`.gickup-trash` 中的文件何时清理？

**用户最终答案：不自动清理，由用户在垃圾箱页面恢复或永久删除**

## Q69

**问题：垃圾箱重名处理**：删除同名文件多次时如何避免覆盖？

**用户最终答案：垃圾文件名追加 UTC 时间戳和短随机后缀，并记录原文件名**

## Q70

**问题：文件名大小写冲突**：Windows 文件系统通常不区分大小写。新建或重命名时如何检测冲突？

**用户最终答案：按不区分大小写比较，拒绝与目录内现有文件冲突**

## Q71

**问题：Schema 提供方式**：前端所用 `gickup_spec.json` 从哪里加载？

**用户最终答案：后端从二进制内嵌 Schema 提供只读 API**

## Q72

**问题：UI Schema 归属**：RJSF 的 `uiSchema` 和字段呈现规则放在哪里维护？

**用户最终答案：前端 TypeScript 代码中，按配置路径组织**

## Q73

**问题：Schema 更新责任**：以后新增 Go 配置字段时，什么条件才能通过 CI？

**用户最终答案：Go 模型、Schema、示例及必要的 UI 呈现测试必须同步更新**

## Q74

**问题：Bootstrap 3 定制程度**：已选择 RJSF 默认 Bootstrap 3 主题，界面视觉采用什么策略？

**用户最终答案：保留 RJSF 控件行为，但用少量项目 CSS 构建桌面工具布局**

## Q75

**问题：主界面信息架构**：桌面端首版采用哪种布局？

**用户最终答案：左侧文件与文档树，右侧表单**

## Q76

**问题：未保存修改导航保护**：用户切换文件、文档或关闭页面时如何处理？

**用户最终答案：阻止导航并提示保存、放弃或取消**

## Q77

**问题：表单组织方式**：大型配置文档包含大量来源和目标，如何避免单个超长表单？

**用户最终答案：按顶层区域分段，并将各 provider 项做成可折叠卡片**

## Q78

**问题：高级字段展示**：不常用字段首版如何呈现？

**用户最终答案：根据 `uiSchema` 标记为高级项，默认折叠但可展开**

## Q79

**问题：帮助文本来源**：字段说明以什么为主？

**用户最终答案：Schema 描述为单一基础来源，`uiSchema` 仅补充界面操作提示**

## Q80

**问题：前端测试层次**：首版最低测试组合是什么？

**用户最终答案：Vitest 组件测试、API 客户端测试，加少量 Playwright Edge 端到端测试**

## Q81

**问题：后端测试层次**：Go 侧最低需要覆盖哪些内容？

**用户最终答案：路径安全、扫描、YAML AST 往返、校验、原子写、冲突、备份、垃圾箱和 HTTP API**

## Q82

**问题：浏览器端到端环境**：CI 中如何测试“仅支持当前稳定版 Edge”？

**用户最终答案：用 Chromium 代替 Edge 即视为等价**

## Q83

**问题：Node 工具链锁定**：前端采用哪个包管理器和版本约束？

**用户最终答案：npm，提交 `package-lock.json`，在 `package.json` 中声明 Node `>=20`**

## Q84

**问题：构建失败策略**：Go 构建时若缺少前端产物，应如何处理？

**用户最终答案：正式构建必须先生成前端产物并明确失败；开发测试可使用独立目标**

## Q85

**问题：构建产物入库**：Vite 的 `dist` 是否提交到 Git？

**用户最终答案：不提交，由 CI 和发布流程从锁定依赖构建**

## Q86

**问题：发布兼容要求**：新增 WebUI 后，现有无 Node 环境的终端用户是否仍只需下载一个 Gickup 二进制？

**用户最终答案：是，Node 只存在于源码构建和发布流水线**
